init {
    a, b, c, x, y : Float
    variable1, variable2, i, j : Int
    p1, p2, p3, mensaje : String
    condicion : Float
}

*- Ejemplo de asignaciones-*
a := 5
b := 10
c := a + b

*- Ejemplo de if -*
if (a <= b AND c >= b)
{
    write("a a b")
}

